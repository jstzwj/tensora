INCLUDE_DIRECTORIES(${TENSORA_PATH_INCLUDE})
INCLUDE_DIRECTORIES(${TENSORA_PATH_INSTALL}/include)

SET(TEST_PATH
    ${TENSORA_PATH_TEST}/Test.cpp
    ${TENSORA_PATH_TEST}/TensorStorageTest.h
    ${TENSORA_PATH_TEST}/DimVectorTest.h
)
ADD_EXECUTABLE(unit_test ${TEST_PATH})